See http://en.wikipedia.org/wiki/Langton's_ant .
gcc and make should be enough to compile it.

See antsim -?
If you specify a bounding box on the cmdline, you'll need to give it again if re-running the sim to get the same results
